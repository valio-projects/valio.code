// Package structuregraph builds bounded, syntactic declaration graphs from a
// language helper report. It records only parser observations and never claims
// name, type, member, import, or call resolution.
package structuregraph
