# Generate your own ignored .env with scripts/setup.ps1 or scripts/setup.sh.
SURREAL_ROOT_PASSWORD=replace-with-at-least-32-random-characters
VALIO_API_DB_PASSWORD=replace-with-a-different-random-password
VALIO_WORKER_DB_PASSWORD=replace-with-a-different-random-password
VALIO_API_TOKEN=replace-with-at-least-32-random-characters
